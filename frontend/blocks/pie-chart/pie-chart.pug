mixin pie-chart(items)
  - var itemsStr = ''
  each item in items.length ? items : [ 1 ]
    - itemsStr += item + ','
  - itemsStr = itemsStr.slice(0, itemsStr.length - 1)

  .pie-chart
    span.pie-chart__items #{itemsStr}